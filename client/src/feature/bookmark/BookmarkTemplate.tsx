export default function BookmarkTemplate() {
  // const { posts } = props;
  return (
    <div>
      {/*{posts.map((post) => (*/}
      {/*  <div key={post.id}>{JSON.stringify(post)}</div>*/}
      {/*))}*/}
    </div>
  );
}
